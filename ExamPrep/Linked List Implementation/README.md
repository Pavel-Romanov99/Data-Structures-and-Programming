This is a linked list implementation with these capabilities:
	void addBeginning(int x);
	void addAt(int x, int position);
	void addEnd(int x);
	void print();
	void removeBiginning();
	void removeEnd();
	void removeAt(int position);
	int length();
